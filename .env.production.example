# ============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================================================
# 
# CRITICAL: This file contains sensitive configuration. 
# - DO NOT commit this file to version control
# - Store securely (e.g., in a secrets manager like AWS Secrets Manager, HashiCorp Vault)
# - Rotate secrets regularly
# - Use strong, randomly generated values for all secrets
#
# ============================================================================

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------

# Application name
APP_NAME=myportal

# Environment (MUST be 'production' for production deployments)
NODE_ENV=production

# Base URL of your application (e.g., https://portal.yourcompany.com)
# REQUIRED: Set this to your actual domain
BASE_URL=https://your-domain.com

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------

# PostgreSQL credentials
# CRITICAL: Change these from defaults! Use strong, unique passwords.
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD_12345
DB_NAME=app

# Full database URL (constructed from above, or set directly)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}

# ----------------------------------------------------------------------------
# Security & Authentication
# ----------------------------------------------------------------------------

# Admin password for system administration
# CRITICAL: Use a strong, randomly generated password (min 32 characters)
# Generate with: openssl rand -base64 32
ADMIN_PASSWORD=CHANGE_ME_STRONG_RANDOM_PASSWORD_32_CHARS

# JWT secret for token signing
# CRITICAL: Use a strong, randomly generated secret (min 64 characters)
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_STRONG_RANDOM_SECRET_64_CHARS

# ----------------------------------------------------------------------------
# MinIO Object Storage
# ----------------------------------------------------------------------------

# MinIO credentials (used for file storage)
# CRITICAL: Change these from defaults!
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD_12345

# MinIO ports (optional, defaults shown)
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# ----------------------------------------------------------------------------
# External Services
# ----------------------------------------------------------------------------

# OpenAI API Key for AI features
# REQUIRED: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY_HERE

# ----------------------------------------------------------------------------
# Network Configuration
# ----------------------------------------------------------------------------

# Nginx port (external port for the application)
NGINX_PORT=80

# ----------------------------------------------------------------------------
# SECURITY CHECKLIST BEFORE DEPLOYMENT:
# ----------------------------------------------------------------------------
# [ ] Changed all passwords from defaults
# [ ] Generated strong random values for JWT_SECRET and ADMIN_PASSWORD
# [ ] Set BASE_URL to your actual domain
# [ ] Added valid OPENAI_API_KEY
# [ ] Configured DATABASE_URL with secure credentials
# [ ] Set up SSL/TLS certificates (use Let's Encrypt or your certificate provider)
# [ ] Configured firewall rules to restrict access
# [ ] Set up database backups
# [ ] Configured log aggregation and monitoring
# [ ] Tested disaster recovery procedures
# ============================================================================

version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Build started on `date`"
      - docker build -f docker/Dockerfile.production -t myportal .
run:
  runtime-version: latest
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "3000"
    - name: DATABASE_URL
      value: "postgresql://postgres:Cloud202space@myportal-db.c3c20wk6k62v.eu-west-2.rds.amazonaws.com:5432/myportal"
    - name: ADMIN_PASSWORD
      value: "admin123"
    - name: JWT_SECRET
      value: "cloud202-super-secure-jwt-secret-key-for-production-use-2024"
    - name: OPENAI_API_KEY
      value: "sk-proj-placeholder-add-your-key-if-needed"
